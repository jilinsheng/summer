/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mingda.action.organ;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.struts.Globals;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.hibernate.Session;

import com.mingda.common.SessionFactory;
import com.mingda.dao.SysTOrganizationDAO;
import com.mingda.entity.SysTOrganization;

/**
 * MyEclipse Struts Creation date: 10-17-2008
 * 
 * XDoclet definition:
 * 
 * @struts.action validate="true"
 * @struts.action-forward name="organstop" path="/page/organ/organstop.jsp"
 */
public class OrganstopinitAction extends Action {
	/*
	 * Generated Methods
	 */

	/**
	 * Method execute
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		String organizationId = request.getParameter("organizationId");
		String type = request.getParameter("type");
		Session session = SessionFactory.getSession();
		SysTOrganization organ = new SysTOrganization();
		try {
			SysTOrganizationDAO odao = new SysTOrganizationDAO();
			organ = odao.findById(organizationId);
			if ("1".equals(organ.getStatus())) {
				request.setAttribute("button", "停用");
			} else {
				request.setAttribute("button", "启用");
			}
			request.setAttribute("organ", organ);
			request.setAttribute("title", "停用下属机构");
		} catch (Exception e) {
			e.printStackTrace();
		} finally {
			session.close();
		}
		this.saveToken(request);
		Globals s;
		return mapping.findForward("organstop");
	}
}