/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mingda.action.info.move;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.hibernate.Session;
import org.hibernate.Transaction;

import com.mingda.common.SessionFactory;
import com.mingda.common.log4j.Log4jApp;
import com.mingda.entity.SysTEmployee;

/**
 * MyEclipse Struts Creation date: 08-25-2008
 * 
 * XDoclet definition:
 * 
 * @struts.action validate="true"
 */
public class MoveexternalininitAction extends Action {
	/*
	 * Generated Methods
	 */

	/**
	 * Method execute
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		String familyid = request.getParameter("familyid");
		String mid = request.getParameter("mid");
		Session session = SessionFactory.getSession();
		Connection con = null;
		PreparedStatement ps = null;
		ResultSet rs = null;
		String sql = "";
		String oldOrgId = "";
		String html = "<input name=\"familyId\" type=\"hidden\" value=\""
				+ familyid
				+ "\" />"
				+ "<table width=\"95%\" border=\"0\" cellspacing=\"4\" cellpadding=\"0\">";
		Transaction tx =session.beginTransaction();
		try {
			con = session.connection();
			/**
			 * 验证是否可以进行迁移 证件 业务 判断
			 */
			//sql = " select * from biz_t_optcheck t where  t.family_id='"
			//		+ familyid + "'";
			//Log4jApp.logger(sql);
			//ps = con.prepareStatement(sql);
			//rs = ps.executeQuery();
			boolean flag=false;
			//while(rs.next()){
			//	flag=true;
			//}
			if (flag) {
				request.setAttribute("result", "请撤销所有业务，再进行迁移!");
			}else{
				String registeredaddress="";
				String familyaddress ="";
				sql = "select fam.*,org.orgname,org.fullname "
						+ " from info_t_family fam, sys_t_organization org "
						+ "where fam.family_id = '" + familyid + "' "
						+ " and org.organization_id = fam.organization_id";
				Log4jApp.logger(sql);
				ps = con.prepareStatement(sql);
				rs = ps.executeQuery();
				if (rs.next()) {
					//去机构
					HttpSession hsession =request.getSession();
					SysTEmployee employee =(SysTEmployee)hsession.getAttribute("employee");
					oldOrgId = employee.getSysTOrganization().getOrganizationId();
				
					 registeredaddress = rs
							.getString("RPRADDRESS");
					if (registeredaddress == null
							|| registeredaddress.equals("")
							|| registeredaddress.equals("null")) {
						registeredaddress = rs.getString("fullname");
					}
					 familyaddress = rs.getString("FAMADDRESS");
					if (familyaddress == null || familyaddress.equals("")
							|| familyaddress.equals("null")) {
						familyaddress = rs.getString("fullname");
					}
					html += "<input name=\"fullname\" type=\"hidden\" value=\""
						+employee.getSysTOrganization().getFullname()+ "\" />";
					html += "<input name=\"orgid\" type=\"hidden\" value=\""
							+ oldOrgId + "\" />";
					html += "<input name=\"famno\" type=\"hidden\" value=\""
						+ rs.getString("familyno") + "\" />";
					html += "<input name=\"fammoveid\" type=\"hidden\" value=\""
						+ mid + "\" />";
					html += "<tr valign=\"middle\"><td width=\"80\" height=\"25\">原家庭编号</td><td>"
							+ rs.getString("familyno") + " </td></tr>";
					html += "<tr valign=\"middle\"><td width=\"80\" height=\"25\">户主姓名</td><td>"
							+ rs.getString("mastername") + "</td></tr>";
					html += "<tr valign=\"middle\"><td width=\"80\" height=\"25\">原所属</td><td>"
						+ rs.getString("fullname")+ " </td></tr>";
					
				}
				sql = "select * from sys_t_organization org where org.parentorgid = substr('"
						+ oldOrgId
						+ "', 1, 6)  order by org.organization_id";
				ps = con.prepareStatement(sql);
				rs = ps.executeQuery();
				html += "<tr valign=\"middle\"><td width=\"120\" height=\"25\">选择所属</td><td valign=\"middle\"> 乡（镇、街道）&nbsp;&nbsp;<select id=\"street\" onchange=\"readnext(this.options[this.options.selectedIndex].value)\"><option value=\"\">未选择...</option>";
				while (rs.next()) {
					html += "<option value=\""
							+ rs.getString("organization_id") + "\">"
							+ rs.getString("orgname") + "</option>";
				}
				html += "</select>&nbsp;&nbsp;&nbsp;&nbsp;<span id=\"aaa\">村屯（社区）&nbsp;&nbsp;<select id=\"community\" name=\"community\"><option value=\"\">未选择...</option></select></span></td></tr>";
				html += "<tr valign=\"middle\"><td width=\"80\" height=\"25\">户口地址</td><td><input name=\"registeredaddress\" size=\"60\" type=\"text\" value=\""
					+ registeredaddress + "\" /></td></tr>";
				html += "<tr valign=\"middle\"><td width=\"80\" height=\"25\">居住地址</td><td><input name=\"familyaddress\" size=\"60\" type=\"text\" value=\""
					+ familyaddress + "\" /></td></tr>";
				html+="</table>";
				request.setAttribute("html", html);
			}
			tx.commit();
		} catch (SQLException e) {
			tx.rollback();
			e.printStackTrace();
		} finally {
			try {
				if (rs != null) {
					rs.close();
				}
				if (ps != null) {
					ps.close();
				}
				if (con != null) {
					con.close();
				}
				session.close();
			} catch (SQLException e) {
			}
		}
		
		return mapping.findForward("moveexternalin");
	}
}