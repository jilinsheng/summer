/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mingda.action.policy.check;

import java.io.IOException;
import java.io.PrintWriter;
import java.io.UnsupportedEncodingException;
import java.util.HashMap;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

/** 
 * MyEclipse Struts
 * Creation date: 11-17-2008
 * 
 * XDoclet definition:
 * @struts.action validate="true"
 */
public class PolicycheckmanageAction extends Action {
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		// 获取action
		String action = request.getParameter("action");
		//
		response.setContentType("text/html; charset=UTF-8");
		
		try {
			request.setCharacterEncoding("UTF-8");
		} catch (UnsupportedEncodingException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		
		//
		try {
			PrintWriter out = response.getWriter();
			//单个、批量审批
			if(action.equals("setMoreCheckIdea")){
				//单个或批量审批
				String pid = request.getParameter("pid");	
				String allaccflag = request.getParameter("allaccflag");	
				String preportflag = request.getParameter("preportflag");	
				String empid = request.getParameter("empid");	
				//
				String reqtype = request.getParameter("reqtype");	
				String reptype = request.getParameter("reptype");	
				String repman = request.getParameter("repman");	
				String repidea = request.getParameter("repidea");	
				String repdt = request.getParameter("repdt");	
				String repbegdt = request.getParameter("repbegdt");
				String rependdt = request.getParameter("rependdt");
				//
				String idsmoneys = request.getParameter("idsmoneys");				
				//分类施保审批	
				String idschildmoneys = request.getParameter("idschildmoneys");
				//
				//评议属性
    			HashMap hashmap = new HashMap();
    			hashmap.put("pid", pid);
    			hashmap.put("allaccflag", allaccflag);
    			hashmap.put("preportflag", preportflag);
    			hashmap.put("empid", empid);
    			//
    			hashmap.put("reqtype", reqtype);
    			hashmap.put("reptype", reptype);
    			hashmap.put("repman", repman);
    			hashmap.put("repidea", repidea);
    			hashmap.put("repdt", repdt);
    			hashmap.put("repbegdt", repbegdt);
    			hashmap.put("rependdt", rependdt);
    			//
    			hashmap.put("idsmoneys", idsmoneys);
    			//分类施保审批	
    			hashmap.put("idschildmoneys", idschildmoneys);
    			//业务审批管理类
    			Policycheckmanage policycheckmanage = new Policycheckmanage();  			
    			out.print(policycheckmanage.setMoreCheckIdea(hashmap));
			}else if(action.equals("setAllCheckIdeaFamily")){
				//全部(sql语句)审批
				//单个或批量审批
				String pid = request.getParameter("pid");	
				String allaccflag = request.getParameter("allaccflag");	
				String preportflag = request.getParameter("preportflag");	
				String empid = request.getParameter("empid");	
				//
				String reqtype = request.getParameter("reqtype");	
				String reptype = request.getParameter("reptype");	
				String repman = request.getParameter("repman");	
				String repidea = request.getParameter("repidea");	
				String repdt = request.getParameter("repdt");	
				String repbegdt = request.getParameter("repbegdt");
				String rependdt = request.getParameter("rependdt");
				//
				String selsql = request.getParameter("selsql");
				//
				//评议属性
    			HashMap hashmap = new HashMap();
    			hashmap.put("pid", pid);
    			hashmap.put("allaccflag", allaccflag);
    			hashmap.put("preportflag", preportflag);
    			hashmap.put("empid", empid);
    			//
    			hashmap.put("reqtype", reqtype);
    			hashmap.put("reptype", reptype);
    			hashmap.put("repman", repman);
    			hashmap.put("repidea", repidea);
    			hashmap.put("repdt", repdt);
    			hashmap.put("repbegdt", repbegdt);
    			hashmap.put("rependdt", rependdt);
    			//
    			hashmap.put("selsql", selsql);
    			//业务审批管理类
    			Policycheckmanage policycheckmanage = new Policycheckmanage(); 			
    			out.print(policycheckmanage.setAllCheckIdeaFamily(hashmap));
			}else{
				out.print("xiuxiu");
			}			
		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		return null;
	}
}