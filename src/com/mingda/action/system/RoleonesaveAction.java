/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mingda.action.system;

import java.io.IOException;
import java.io.PrintWriter;
import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.hibernate.Session;
import org.hibernate.Transaction;

import com.mingda.common.log4j.Log4jApp;

/**
 * MyEclipse Struts Creation date: 08-23-2008
 * 
 * XDoclet definition:
 * 
 * @struts.action validate="true"
 */
public class RoleonesaveAction extends Action {
	/*
	 * Generated Methods
	 */

	/**
	 * Method execute
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		String name = request.getParameter("name");
		String detail = request.getParameter("detail");
		String[] items = request.getParameterValues("item");
		String roleId = request.getParameter("roleId");
		String privilegeids = "'";
		boolean hasPrivilege = true;
		if (null == items || 0 == items.length) {
			hasPrivilege = false;
		} else {
			for (int i = 0; i < items.length; i++) {
				privilegeids += items[i] + "','";
			}
			privilegeids += "'";
		}
		Connection con = null;
		PreparedStatement ps = null;
		ResultSet rs = null;
		String sql = "";
		Session session = com.mingda.common.SessionFactory.getSession();
		Transaction tx = session.beginTransaction();
		try {
			con = session.connection();

			if (roleId.equals("-1")) {
				sql = "select xrole_id.nextval from dual";
				ps = con.prepareStatement(sql);
				rs = ps.executeQuery();
				if (rs.next()) {
					roleId = rs.getString(1);
				}
				sql = "insert into sys_t_role (role_id, code, name, detail, sequence, status) values "
						+ "('"
						+ roleId
						+ "', '"
						+ roleId
						+ "', '"
						+ name
						+ "', '" + detail + "', '" + roleId + "', '1')";
			} else {
				sql = "update sys_t_role set name ='" + name + "', detail = '"
						+ detail + "' where role_id = '" + roleId + "'";
			}
			Log4jApp.logger(sql);
			ps = con.prepareStatement(sql);
			ps.execute();
			sql = "delete  from sys_t_power p where p.role_id='" + roleId + "'";
			Log4jApp.logger(sql);
			ps = con.prepareStatement(sql);
			ps.execute();
			if (hasPrivilege) {
				sql = "insert into sys_t_power (privilege_id, role_id) select pri.privilege_id, '"
						+ roleId
						+ "' from sys_t_privilege pri where pri.privilege_id in ("
						+ privilegeids + ")";
				Log4jApp.logger(sql);
				ps = con.prepareStatement(sql);
				ps.execute();
			}
			tx.commit();
		} catch (Exception ex) {
			tx.rollback();
			ex.printStackTrace();
		} finally {
			try {
				if (rs != null) {
					rs.close();
				}
				if (ps != null) {
					ps.close();
				}
				if (con != null) {
					con.close();
				}
				session.close();
			} catch (SQLException e) {
			}
		}
		response.setContentType("text/html");
		response.setCharacterEncoding("GB18030");

		try {
			PrintWriter out = response.getWriter();
			out
					.write("<script>alert('"
							+ name
							+ " ±£´æ³É¹¦');parent.frames['leftFrame'].location.reload('rolelist.do?roleId="+roleId+"');</script>");
			out.flush();
			out.close();
		} catch (IOException e) {
			e.printStackTrace();
		}

		return null;
	}
}